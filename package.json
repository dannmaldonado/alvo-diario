{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/api\"",
    "build": "rm -rf dist && npm run build --prefix apps/web",
    "start": "npm run build && npm run start --prefix apps/api",
    "lint": "npm run lint --prefix apps/web",
    "migrate": "npm run migrate --prefix apps/api"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "workspaces": [
    "apps/*"
  ]
}
