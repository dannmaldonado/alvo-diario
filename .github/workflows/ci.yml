name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint
        continue-on-error: true

      - name: Build web app
        run: npm run build

  build-pocketbase:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify PocketBase binary
        run: |
          if [ -f apps/pocketbase/pocketbase ]; then
            echo "✓ PocketBase binary found"
          else
            echo "✗ PocketBase binary not found"
            exit 1
          fi

  branch-protection:
    needs: [lint-and-test, build-pocketbase]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check CI status
        run: |
          if [ "${{ needs.lint-and-test.result }}" = "success" ] && [ "${{ needs.build-pocketbase.result }}" = "success" ]; then
            echo "✓ All checks passed"
          else
            echo "✗ Some checks failed"
            exit 1
          fi
